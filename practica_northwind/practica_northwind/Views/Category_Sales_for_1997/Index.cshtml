@model IEnumerable<practica_northwind.Models.Category_Sales_for_1997>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategorySales)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategorySales)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryName }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoryName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryName })
        </td>
    </tr>
}

</table>
